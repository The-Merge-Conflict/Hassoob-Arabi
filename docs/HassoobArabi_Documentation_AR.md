# حاسوب عربي (Hassoob Arabi)

> **لغة برمجة عربية بالكامل** ومترجمها/مفسّرها، مستوحاة من Wolfram Mathematica: بنية لغوية عربية بالكامل، محرك رياضي رمزي وعددي، رسوم بيانية، رسائل أخطاء عربية لطيفة، وبيئة تطوير يمينية الاتجاه (RTL).

**الفريق:** جعفر محفوض · ليلى العبد الله · حاتم إبراهيم
**المستودع:** https://github.com/The-Merge-Conflict/Hassoob-Arabi

📖 التوثيق الكامل بالإنجليزية: [`HassoobArabi_Documentation_EN.md`](HassoobArabi_Documentation_EN.md) · التوثيق الكامل بالعربية: [`HassoobArabi_Documentation_AR.md`](HassoobArabi_Documentation_AR.md)

---

## جدول المحتويات
1. [نظرة عامة](#١-نظرة-عامة)
2. [ما الجديد — إعادة الهيكلة المعيارية](#٢-ما-الجديد--إعادة-الهيكلة-المعيارية)
3. [البنية ومسار الترجمة](#٣-البنية-ومسار-الترجمة)
4. [هيكل المشروع](#٤-هيكل-المشروع)
5. [التثبيت والتشغيل](#٥-التثبيت-والتشغيل)
6. [مرجع اللغة](#٦-مرجع-اللغة)
7. [فهرس الدوال المدمجة](#٧-فهرس-الدوال-المدمجة)
8. [معالجة الأخطاء](#٨-معالجة-الأخطاء)
9. [البرامج الأمثلة](#٩-البرامج-الأمثلة)
10. [الاختبارات](#١٠-الاختبارات)
11. [المستودع والفريق](#١١-المستودع-والفريق)

---

## ١. نظرة عامة

**حاسوب عربي** (*"الحاسوب العربي"*) هو لغة برمجة كاملة تكون فيها **الكلمات المفتاحية والمعرّفات والأرقام ورسائل الأخطاء كلها بالعربية**، وتستخدم ملفاتها المصدرية الامتداد `.حع`. وهي مستوحاة من Wolfram Mathematica: فإلى جانب البرمجة الأمرية الاعتيادية، تتعامل مع **الرياضيات الرمزية** كمواطن من الدرجة الأولى — إذ يمكنك تعريف الرموز، والاشتقاق، والتكامل، وأخذ النهايات، ونشر متسلسلات تايلور، وحل المعادلات، والتعامل مع المصفوفات، وحساب الإحصاءات، ورسم المخططات، وكل ذلك باستخدام أوامر عربية.

الخصائص الرئيسية:

- **بنية عربية** — كلمات التحكم بالتدفق (`إذا`، `بينما`، `لكل`، `دالة`، …)، والدوال المدمجة (`اطبع`، `اشتق`، `متوسط`، …)، والعوامل تُقرأ بشكل طبيعي بالعربية.
- **أرقام عربية-هندية** — يُقبل كل من `٠١٢٣` و`0123` في الشيفرة المصدرية، و**كل المخرجات تُطبع بالأرقام العربية** (٠–٩).
- **محرك رمزي + عددي** — الجبر/التفاضل والتكامل الرمزي عبر SymPy، والعمليات العددية/الجبر الخطي السريعة عبر NumPy، والرسم البياني عبر Matplotlib.
- **أخطاء لطيفة** — كل خطأ هو رسالة عربية بالكامل وظريفة بالصيغة المعيارية `خطأ في السطر {السطر}، العمود {العمود}: …`، وغالبًا مع اقتراح *"هل تقصد…"* (`هل تقصد`).
- **بيئة تطوير يمينية (RTL)** — محرر يميني الاتجاه مع تلوين بنيوي، وتشغيل فوري، وصدفة تفاعلية (REPL).

---

## ٢. ما الجديد — إعادة الهيكلة المعيارية

قُسِّمت الملفات المصدرية الأربعة الأكبر إلى **حزم فرعية مركّزة** لتحسين التجريد وسهولة القراءة. وتعرض كل حزمة ملف `__init__.py` **يعيد تصدير كامل الواجهة العامة**، بحيث يبقى كل استيراد قائم (`from errors import …`، `from ast_nodes import …`، `from interpreter import …`) يعمل دون تغيير — **لم تُعدَّل أي مواضع استدعاء ولا أي اختبارات**، و**لا يوجد أي تغيير في السلوك**.

| قبل (ملف واحد) | بعد (حزمة) | الوحدات الفرعية |
|---|---|---|
| `src/errors.py` | `src/errors/` | `base` (المساعدات + `HassoobError`)، `syntax` (`LexError`، `ParseError`)، `semantic` (`SemanticError`، `MultiError`)، `runtime` (`HassoobRuntimeError`)، `signals` (`ReturnSignal`/`BreakSignal`/`ContinueSignal`) |
| `src/ast_nodes.py` | `src/ast_nodes/` | `base` (المزيج `_Positioned`)، `statements`، `expressions`، `literals` |
| `src/interpreter.py` | `src/interpreter/` | `evaluator` (قيمة `Function` + `Interpreter`)، `parsing` (واجهة ANTLR الأمامية، وإزالة التشكيل، وأخطاء نحوية عربية لطيفة، `parse_program`/`parse_expression`)، `pipeline` (`run_source`) |
| `src/builtins.py` | `src/library/` | `formatting`، `symbolic`، `numeric`، `statistics`، `text_lists`، `plotting`، بالإضافة إلى مساعدات `_shared`؛ ويجمّع `_builtins_loader.py` سجلّ `BUILTINS` من هذه المجالات |

**لماذا يفيد ذلك:** صارت الأخطاء الآن مجمّعة حسب *النوع* (لفظي/نحوي مقابل دلالي مقابل تنفيذي)، وفُصلت إشارات التحكم بالتدفق بنظافة عن الأخطاء الحقيقية، ونُظِّمت عقد الـ AST حسب الفئة، وعُزلت واجهة التحليل الأمامية للمفسّر عن التقييم، وقُسّمت المكتبة القياسية حسب المجال الرياضي.

---

## ٣. البنية ومسار الترجمة

حاسوب عربي هو **مفسّر يجوب الشجرة (tree-walking interpreter)** مبني على واجهة أمامية من ANTLR4. ويمرّ الملف المصدري عبر المراحل التالية:

```
  source.حع
      │
      ▼
┌─────────────┐   محلل لفظي + نحوي مولَّد بواسطة ANTLR4 من HassoobArabi.g4
│  الواجهة    │   ← ينتج شجرة إعراب ملموسة
└─────────────┘
      │  شجرة الإعراب
      ▼
┌─────────────┐   يجوب ast_builder.py شجرة الإعراب
│  باني AST   │   ← ينتج شجرة تركيب مجردة نظيفة (ast_nodes/)
└─────────────┘
      │  الـ AST
      ▼
┌─────────────┐   semantic.py: فحص المتغيرات غير المعرّفة، وفحص عدد الوسطاء،
│   الدلالة   │   ومواضع break/continue/return، وتلميحات "هل تقصد"
└─────────────┘
      │  AST مُتحقَّق منه
      ▼
┌─────────────┐   optimizer.py: طيّ الثوابت، والتبسيط الجبري،
│   المُحسِّن   │   وإزالة الفروع الميتة
└─────────────┘
      │  AST مُحسَّن
      ▼
┌─────────────┐   يجوب interpreter/ الـ AST؛ ويحمل environment.py
│   المفسّر    │   سلسلة النطاقات؛ وتوفّر library/ المكتبة القياسية
└─────────────┘
      │
      ▼
  مخرجات الطرفية (بأرقام عربية) + نوافذ مخططات Matplotlib
```

يُربط المسار كله معًا بواسطة `interpreter/pipeline.py:run_source` (وكذلك `main.py`): **التحليل ← التحسين ← التحليل الدلالي ← التفسير**.

**مسؤوليات المراحل**

| المرحلة | الوحدة (الوحدات) | المسؤولية |
|---|---|---|
| التحليل اللفظي والنحوي | `generated/` (من `HassoobArabi.g4`)، `src/interpreter/parsing.py` | تجزئة المصدر العربي إلى رموز، وبناء شجرة إعراب، وترجمة أخطاء ANTLR إلى أخطاء عربية لطيفة. |
| بناء الـ AST | `src/ast_builder.py`، `src/ast_nodes/` | تحويل شجرة الإعراب إلى عقد AST مُنمَّطة. |
| التحليل الدلالي | `src/semantic.py` | فحوص ساكنة: الأسماء غير المعرّفة، وعدد الوسطاء الخاطئ، وسوء وضع `إرجع`/`اوقف`/`استمر`. |
| التحسين | `src/optimizer.py` | طيّ الثوابت، والتبسيط، وإزالة الفروع غير القابلة للوصول. |
| التفسير | `src/interpreter/evaluator.py`، `src/environment.py` | تنفيذ الـ AST؛ وإدارة سلسلة النطاقات (على نمط Mathematica). |
| المكتبة القياسية | `src/library/`، `src/_builtins_loader.py`، `src/runtime_ops.py`، `src/lang_utils.py` | الدوال المدمجة، ودلالات العوامل، والمساعدات. |
| الأخطاء | `src/errors/` | تسلسل هرمي عربي لطيف للاستثناءات + إشارات التحكم بالتدفق. |
| الأدوات | `src/repl.py`، `ide/` | الصدفة التفاعلية (REPL) وبيئة التطوير الرسومية اليمينية (RTL). |

---

## ٤. هيكل المشروع

```
HassoobArabi/
├─ HassoobArabi.g4         قواعد ANTLR4 (تعريف اللغة)
├─ generated/             المحلل اللفظي/النحوي المولَّد من ANTLR4 (ناتج بناء)
├─ main.py                نقطة الدخول (تشغيل ملف / REPL / -c / --ide)
├─ requirements.txt       اعتماديات بايثون
├─ src/
│  ├─ ast_nodes/          أصناف عقد الـ AST (حزمة)
│  │  ├─ __init__.py        يعيد تصدير كل عقدة (يحافظ على `from ast_nodes import *`)
│  │  ├─ base.py            المزيج _Positioned لموقع المصدر
│  │  ├─ statements.py      عقد الجُمل / التحكم بالتدفق
│  │  ├─ expressions.py     عقد التعابير (العوامل، الاستدعاءات، الفهرسة، اللامبدا)
│  │  └─ literals.py        عقد القيم الحرفية / الثوابت (الأرقام، النصوص، القوائم، π/e/∞)
│  ├─ ast_builder.py      شجرة الإعراب ← AST
│  ├─ semantic.py         التحليل الدلالي الساكن
│  ├─ optimizer.py        مُحسِّن الـ AST
│  ├─ interpreter/        المفسّر الجائب للشجرة (حزمة)
│  │  ├─ __init__.py        يعيد تصدير Interpreter وFunction وparse_program وparse_expression وrun_source
│  │  ├─ evaluator.py       قيمة Function + صنف Interpreter
│  │  ├─ parsing.py         واجهة ANTLR الأمامية + أخطاء نحوية عربية لطيفة
│  │  └─ pipeline.py        run_source: التحليل ← التحسين ← التحليل الدلالي ← التفسير
│  ├─ environment.py      سلسلة النطاقات
│  ├─ library/            المكتبة القياسية (حزمة، تحل محل builtins.py)
│  │  ├─ __init__.py        يجمّع سجلّ BUILTINS من جميع المجالات
│  │  ├─ _shared.py         مساعدات مشتركة (تحويل الأعداد، الكائنات القابلة للاستدعاء، …)
│  │  ├─ formatting.py      تنسيق المخرجات / القيم (format_value، to_arabic_digits)
│  │  ├─ symbolic.py        الرياضيات الرمزية (الاشتقاق، التكامل، الحل، …)
│  │  ├─ numeric.py         العمليات العددية والجبر الخطي
│  │  ├─ statistics.py      الإحصاء
│  │  ├─ text_lists.py      دوال القوائم والنصوص
│  │  └─ plotting.py        الرسم البياني (Matplotlib آمن بلا واجهة)
│  ├─ _builtins_loader.py يحمّل BUILTINS من library/ (يتجنّب التعارض مع builtins القياسية)
│  ├─ runtime_ops.py      مساعدات زمن التشغيل للعوامل / الفهرسة
│  ├─ lang_utils.py       أدوات مشتركة
│  ├─ errors/             التسلسل الهرمي للأخطاء العربية + إشارات التحكم (حزمة)
│  │  ├─ __init__.py        يعيد تصدير كامل الواجهة العامة
│  │  ├─ base.py            to_arabic_digits، closest_name، HassoobError
│  │  ├─ syntax.py          LexError، ParseError
│  │  ├─ semantic.py        SemanticError، MultiError
│  │  ├─ runtime.py         HassoobRuntimeError
│  │  └─ signals.py         ReturnSignal، BreakSignal، ContinueSignal
│  └─ repl.py             الصدفة التفاعلية
├─ ide/
│  ├─ engine.py           محرك تشغيل/REPL مستقل عن الواجهة
│  ├─ highlight.py        مُلوِّن بنيوي مستقل عن الواجهة
│  ├─ editor_qt.py        محرر PySide6 (Qt) يميني الاتجاه — الأساسي
│  ├─ editor.py           محرر Tkinter يميني الاتجاه — بديل بلا تثبيت
│  ├─ __init__.py         يختار Qt تلقائيًا، ويرجع إلى Tkinter عند الحاجة
│  └─ __main__.py         مُشغِّل `python -m ide`
├─ tests/                 مجموعات اختبارات pytest (تبني أشجار AST مباشرة)
└─ examples/              برامج أمثلة (.حع)
```

---

## ٥. التثبيت والتشغيل

### المتطلبات
- Python 3.10+
- `antlr4-python3-runtime`، `sympy`، `numpy`، `matplotlib`، `scipy`، `prompt_toolkit`، `rich`، `pytest`
- `PySide6` *(اختياري)* — يفعّل محرر Qt اليميني الحقيقي؛ وبدونه ترجع بيئة التطوير إلى Tkinter.

### الإعداد
```bash
pip install -r requirements.txt

# توليد المحلل من القواعد (يتطلب Java + أداة antlr4):
antlr4 -Dlanguage=Python3 -visitor -o generated HassoobArabi.g4
```

### التشغيل
```bash
python main.py                     # صدفة تفاعلية (REPL)
python main.py examples/مرحبا.حع    # تشغيل ملف مصدري
python main.py -c "اطبع(٣+٤)"       # تشغيل تعبير واحد
python main.py --ide               # تشغيل بيئة التطوير الرسومية
python -m ide                      # (مكافئ) تشغيل بيئة التطوير
```

---

## ٦. مرجع اللغة

### ٦.١ الأرقام والتعليقات والجُمل
- **الأرقام:** كل من الأرقام العربية-الهندية (`٠١٢٣٤٥٦٧٨٩`) والغربية (`0-9`) صالحة. يتطلب العدد العشري نقطة ASCII: `٢.٥`.
- **الجُمل** تنتهي عند سطر جديد؛ ويُسمح بفاصلة إنهاء اختيارية `;` أو الفاصلة المنقوطة العربية `؛`.
- **التعليقات:** `# ...` (سطري)، `ملاحظة ...` (تعليق سطري عربي)، `/* ... */` (كتلي).

### ٦.٢ المتغيرات والإسناد
```text
أ = ٧                 # إسناد
أ += ٣                # إسناد مركّب: += -= *= /= %= ^= ++=
النص ++= "!"          # ++= يُلحق بنصّ
```
يستخدم المفسّر **سلسلة نطاقات على نمط Mathematica**: تُدخل الدوالُ نطاقًا جديدًا وتحلّ الأسماء الحرّة مقابل النطاقات المحيطة.

### ٦.٣ أنواع البيانات
| النوع | ما تُرجعه `نوع` | مثال |
|---|---|---|
| عدد | `عدد` | `٣`، `٢.٥`، `-٧` |
| منطقي | `منطقي` | `صح`، `خطأ` |
| نص | `نص` | `"مرحبا"`، `'نص'` |
| فارغ | `فارغ` | `فارغ` |
| قائمة | `قائمة` | `[١, ٢, ٣]` |
| مصفوفة / مصفوفة أعداد | `مصفوفة` | `[[١,٢],[٣,٤]]` (عبر العمليات العددية) |
| رمزي | `رمزي` | ناتج `رمز س` |
| دالة | `دالة` | `دالة` أو لامبدا |

### ٦.٤ العوامل
| الفئة | العوامل |
|---|---|
| حسابية | `+`  `-`  `*`  `/`  `%` (باقي القسمة)  `^` (الأُس)  `÷` (القسمة الصحيحة) |
| المصفوفات | `**` (ضرب المصفوفات) |
| النصوص | `++` (الدمج — يُنسّق الطرفين) |
| المقارنة | `==`  `!=`/`≠`  `<`  `<=`/`≤`  `>`  `>=`/`≥` (قابلة للتسلسل: `٦٠ <= د < ٩٠`) |
| المنطقية | `و`/`&&` (و)  `أو`/`\|\|` (أو)  `ليس`/`!` (ليس) |
| الإسناد | `=`  `+=`  `-=`  `*=`  `/=`  `%=`  `^=`  `++=` |
| سهم اللامبدا | `=>` |

**الثوابت الرمزية:** `باي` / `π`، `هـ` (عدد أويلر *e*)، `لانهاية` (ما لا نهاية).

### ٦.٥ التحكم بالتدفق
```text
# شرط
إذا (أ > ب) {
    اطبع("أكبر")
} وإلا إذا (أ == ب) {
    اطبع("متساوٍ")
} وإلا {
    اطبع("أصغر")
}

# حلقة بينما
بينما (س < ١٠) { س += ١ }

# لكل عنصر في تسلسل
لكل عنصر في [١, ٢, ٣] { اطبع(عنصر) }

# لكل في مدى (شامل للطرفين؛ خطوة اختيارية بخطوة)
لكل ع من ١ إلى ٥ { اطبع(ع) }
لكل ع من ١٠ إلى ١ بخطوة -٢ { اطبع(ع) }
```
`اوقف` = break، `استمر` = continue.

### ٦.٦ الدوال واللامبدا
```text
دالة مربع(س) {
    إرجع س * س
}

دالة عاملي(ن) {
    إذا (ن <= ١) { إرجع ١ }
    إرجع ن * عاملي(ن - ١)     # استدعاء ذاتي (تعاود)
}

# اللامبدا: وسيط واحد بلا أقواس، أو (أ, ب) => ...
مضاعف = س => س * ٢
جمع    = (أ, ب) => أ + ب
```

### ٦.٧ النصوص والنصوص المنسّقة (f-strings)
```text
الاسم = "جعفر"
اطبع("مرحبًا يا " ++ الاسم)            # ++ الدمج
اطبع(f"الطول هو {طول(الاسم)} حروف")   # استيفاء النص المنسّق
```
رموز الهروب: `\n \t \\ \" \' \uXXXX`. تبدأ النصوص المنسّقة بـ `f` أو `F` أو `ف`.

### ٦.٨ القوائم والفهرسة وإسناد الفهرس
```text
أ = [[١, ٢, ٣], [٤, ٥, ٦]]
اطبع(أ[٠][١])          # قراءة فهرس متسلسلة على قائمة  ← ٢
أ[٠][٠] = ٩            # إسناد فهرس متسلسل
م = نقل(أ)             # مصفوفة numpy
اطبع(م[٠, ١])          # فهرسة بفاصلة متعددة الأبعاد — فقط على المصفوفات
```
> **ملاحظة:** صيغة الفاصلة `م[ص, ع]` تنتج مفتاحًا على شكل صفّ (tuple) وتعمل **فقط على المصفوفات** (مثل نواتج `نقل` و`معكوس`). أما للقوائم العادية فاستخدم الفهرسة المتسلسلة `أ[ص][ع]`.

### ٦.٩ الرياضيات الرمزية
```text
رمز س
د = س^٣ - ٦*س^٢ + ٩*س
اطبع("المشتقة:", اشتق(د, س))        # 3·س² − 12·س + 9
اطبع("التكامل:", كامل(د, س))
اطبع("الجذور:", حل(د, س))
اطبع("النهاية:", نهاية((س^٢-١)/(س-١), س, ١))   # 2
```

---

## ٧. فهرس الدوال المدمجة

### الإخراج
| الأمر | الوصف |
|---|---|
| `اطبع(...)` | طباعة قيمة أو أكثر إلى الطرفية (دائمًا بالأرقام العربية). |

### الرياضيات الرمزية
| الأمر | الوصف |
|---|---|
| `بسط(تعبير)` | تبسيط تعبير. |
| `وسع(تعبير)` | النشر (مثل فكّ ضرب الأقواس). |
| `حلل(تعبير)` | تحليل تعبير إلى عوامل. |
| `عوض(تعبير, رمز, قيمة)` | تعويض قيمة عن رمز. |
| `اشتق(تعبير, رمز[, رتبة])` | الاشتقاق (الرتبة اختيارية). |
| `كامل(تعبير, رمز[, أ, ب])` | التكامل — غير محدّد، أو محدّد على `[أ, ب]`. |
| `نهاية(تعبير, رمز, نقطة)` | النهاية عند نقطة. |
| `متسلسلة(تعبير, رمز[, حول, رتبة])` | متسلسلة تايلور/ماكلورين. |
| `حل(معادلة, رمز)` | حل معادلة. |
| `حل_منظومة(معادلات, رموز)` | حل منظومة معادلات. |
| `حل_تفاضلي(معادلة, دالة)` | حل معادلة تفاضلية. |

### العمليات العددية والجبر الخطي
| الأمر | الوصف |
|---|---|
| `جذر_عددي(دالة, رمز, تخمين)` | جذر عددي (طريقة نيوتن). |
| `نقل(مصفوفة)` | المنقول (Transpose). |
| `محدد(مصفوفة)` | المحدِّد (Determinant). |
| `معكوس(مصفوفة)` | المعكوس (Inverse). |
| `قيم_ذاتية(مصفوفة)` | القيم الذاتية (Eigenvalues). |
| `ضرب_نقطي(ش١, ش٢)` | الضرب النقطي (Dot product). |
| `ضرب_متجهي(ش١, ش٢)` | الضرب المتجهي (Cross product). |
| `حل_خطي(أ, ب)` | حل المنظومة الخطية A·x = b. |

### الإحصاء
| الأمر | الوصف |
|---|---|
| `متوسط` | المتوسط الحسابي. |
| `وسيط` | الوسيط. |
| `تباين` | التباين. |
| `انحراف_معياري` | الانحراف المعياري. |
| `أصغر` / `أكبر` | الأصغر / الأكبر. |
| `مدى` | المدى (الأكبر − الأصغر). |
| `مجموع` / `ناتج` | المجموع / حاصل الضرب. |
| `ارتباط(س, ع)` | معامل الارتباط. |
| `عشوائي(...)` | عدد (أو أعداد) عشوائية. |

### الرسم البياني
| الأمر | الوصف |
|---|---|
| `ارسم(تعبير[, مدى])` | رسم تعبير/دالة. |
| `ارسم_وسيط(...)` | رسم وسيطي (parametric). |
| `ارسم_ثلاثي(...)` | رسم ثلاثي الأبعاد. |
| `ارسم_بيانات([xs, ys], نوع="نقاط"/"خط", عنوان=)` | رسم نقاط بيانات / خط. |
| `مدرج_تكراري(بيانات[, عدد], عنوان=)` | مدرّج تكراري (Histogram). |
| `مخطط_أعمدة(...)` | مخطط أعمدة. |

### القوائم والنصوص
| الأمر | الوصف |
|---|---|
| `طول(x)` | الطول. |
| `قطعة(تسلسل, بداية, نهاية)` | اقتطاع (Slice). |
| `استبدل(نص, قديم, جديد)` | استبدال. |
| `قسم(نص[, فاصل])` | تقسيم (Split). |
| `دمج(قائمة[, فاصل])` | دمج (Join). |
| `نوع(x)` | اسم النوع (`عدد`، `نص`، `قائمة`، …). |
| `قائمة_من(بداية, نهاية[, خطوة])` | قائمة مدى (شاملة). |
| `ترتيب(قائمة[, عكسي=])` | ترتيب (تنازليًا اختياريًا). |
| `عكس(تسلسل)` | عكس. |
| `طبق(دالة, قائمة)` | تطبيق (Map). |
| `صفي` / `اختر(دالة, قائمة)` | ترشيح (Filter). |

---

## ٨. معالجة الأخطاء

كل خطأ هو رسالة عربية بالكامل ولطيفة بالصيغة المعيارية:

```
خطأ في السطر {رقم}، العمود {رقم}: {الرسالة}
   ↪ هل تقصد ‹الاقتراح›؟
   💡 {تلميح}
```

يميّز التسلسل الهرمي للأخطاء (المنظَّم الآن تحت `src/errors/`) بين أربع فئات، لكلٍّ منها رمزها التعبيري وتسميتها:

| الفئة | الوحدة | التسمية | الرمز | يُطلق عند |
|---|---|---|---|---|
| لفظي | `errors/syntax.py` (`LexError`) | `خطأ لفظي` | 🔤 | رمز / محرف غير صالح. |
| نحوي | `errors/syntax.py` (`ParseError`) | `خطأ نحوي` | 📝 | مخالفة المصدر للقواعد. |
| دلالي | `errors/semantic.py` (`SemanticError`، `MultiError`) | `خطأ دلالي` | 🔍 | اسم غير معرّف، عدد وسطاء خاطئ، إلخ. |
| تنفيذي | `errors/runtime.py` (`HassoobRuntimeError`) | `خطأ في التنفيذ` | ⚠️ | القسمة على صفر، نوع غير صالح، إلخ. |

عند الخطأ في كتابة اسم، يقدّم المحلل اقتراحًا تقريبيًا *"هل تقصد…"* (`هل تقصد`) باستخدام مطابقة أقرب تطابق، إضافة إلى تلميح اختياري (`💡`). أما إشارات التحكم بالتدفق (`إرجع`/`اوقف`/`استمر`، في `errors/signals.py`) فهي عمدًا **ليست** جزءًا من التسلسل الهرمي للأخطاء، حتى لا تبتلعها معالِجات الأخطاء التي يكتبها المستخدم.

---

## ٩. البرامج الأمثلة

تمارس البرامج الخمسة في `examples/` مجتمعةً اللغة بأكملها. شغّل كلًّا منها بـ `python main.py examples/<الملف>`. *(تُطبع المخرجات بأرقام عربية.)*

- **① `مرحبا.حع` — مقدمة لطيفة.** الطباعة، والمتغيرات، والعوامل الحسابية الستة، وطول النص + الدمج `++` + النصوص المنسّقة، ودالة معرّفة من المستخدم، وشرط `إذا/وإلا`، وحلقة `لكل … من … إلى`.
- **② `إحصاء.حع` — تحليل البيانات.** جميع دوال الإحصاء، والترتيب + الاقتطاع، ولامبدا `اختر`/`طبق`، ودالة تقدير متعددة الفروع، والارتباط، ومخططان (مدرّج تكراري + مبعثر).
- **③ `حساب_تفاضلي.حع` — عُدّة التفاضل والتكامل.** التحليل إلى عوامل، والمشتقتان الأولى/الثانية، والنقاط الحرجة عبر `حل`، واختبار المشتقة الثانية (`عوض`)، والتكاملات غير المحدّدة والمحدّدة، ونهاية 0/0، ومتسلسلة تايلور، وجذر عددي، ورسم `د(س) = س³ − ٦س² + ٩س` مع مشتقتها.
- **④ `مصفوفات.حع` — الجبر الخطي.** ضرب المصفوفات (`**`)، والمنقول، والمحدِّد، والمعكوس، والقيم الذاتية، والفهرسة بالفاصلة متعددة الأبعاد `[ص, ع]`، وإسناد الفهرس المتسلسل، و`حل_خطي`، والضربان النقطي والمتجهي.
- **⑤ `خوارزميات.حع` — التحكم بالتدفق والخوارزميات.** التعاود (العاملي)، و`بينما` + الإسناد المركّب (فيبوناتشي)، واختبار أولية بإرجاع مبكر، و`اوقف`/`استمر`، وكشف التناظر عبر `عكس`، و`قسم`/`دمج`، ولامبدا `طبق`/`اختر`، والمقارنات المتسلسلة، وفحوص النوع `نوع`.

---

## ١٠. الاختبارات

```bash
pytest tests/
# تعمل كل مجموعة أيضًا بشكل مستقل وتبني أشجار AST مباشرة،
# لذا تنجح الاختبارات حتى قبل توليد محلل ANTLR:
python tests/test_interpreter.py
python tests/test_optimizer.py
python tests/test_builtins.py
```

تستورد مجموعات الاختبارات عبر أسماء الحزم (`from interpreter import …`، `from ast_nodes import …`، `from errors import …`)، وهي ما تعيد الحزم الجديدة تصديره دون تغيير — لذا لم تتطلب إعادة الهيكلة المعيارية **أي تغيير في الاختبارات**.

> تلميح: بعد سحب إعادة الهيكلة، امسح البايت-كود القديم إذا واجهت أخطاء استيراد:
> `find . -name __pycache__ -type d -exec rm -rf {} +`

---

## ١١. المستودع والفريق

- **المستودع:** https://github.com/The-Merge-Conflict/Hassoob-Arabi
- **الفريق:** جعفر محفوض · ليلى العبد الله · حاتم إبراهيم
- **المقرر:** المترجمات — مشروع تصميم اللغات وتنفيذها.
