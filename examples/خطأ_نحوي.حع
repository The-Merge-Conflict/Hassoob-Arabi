# خطأ نحوي مقصود: قوس الإغلاق ) ناقص في استدعاء الدالة.
# المتوقع: خطأ نحوي (Parse) يشير إلى نهاية غير متوقعة للملف.

اطبع("مرحبا بالعالم"

# خطأ نحوي مقصود: «إذا» تتطلب قوسين حول الشرط.
# المتوقع: خطأ نحوي عند الرمز > لأن ( مفقودة.

س = ٥
اذا س > ٣ {
    اطبع("كبير")
}