# إحصاء.حع — قائمة من عشرة أعداد وحساب كل المقاييس ثم رسم مدرج تكراري

البيانات = [٤, ٨, ١٥, ١٦, ٢٣, ٤٢, ١٠, ٨, ١٥, ٤]

اطبع("البيانات:", البيانات)
اطبع("المتوسط:", متوسط(البيانات))
اطبع("الوسيط:", وسيط(البيانات))
اطبع("التباين:", تباين(البيانات))
اطبع("الانحراف المعياري:", انحراف_معياري(البيانات))
اطبع("الأصغر:", أصغر(البيانات))
اطبع("الأكبر:", أكبر(البيانات))
اطبع("المدى:", مدى(البيانات))
اطبع("المجموع:", مجموع(البيانات))

مدرج_تكراري(البيانات, ٥, عنوان="التوزيع التكراري")
اطبع("✅ انظر نافذة الرسم لرؤية المدرج التكراري")
