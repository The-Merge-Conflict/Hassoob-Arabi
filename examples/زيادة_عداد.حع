# الإصلاح ٥: الإسناد بـ = داخل دالة يعدّل المتغير الخارجي إن وُجد في السلسلة
# (سلوك شبيه بـ Mathematica).
# المتوقع: العداد = ٣ بعد ثلاث زيادات.

عداد = ٠

دالة زد() {
    عداد = عداد + ١
}

زد()
زد()
زد()
اطبع("العداد بعد ثلاث زيادات:", عداد)
